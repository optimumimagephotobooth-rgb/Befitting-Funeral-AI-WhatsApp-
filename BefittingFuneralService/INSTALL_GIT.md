# 📥 Install Git CLI (Optional)

If you prefer using command-line Git instead of VS Code:

## Download & Install

1. **Download Git for Windows:**
   - Visit: https://git-scm.com/download/win
   - Download the installer

2. **Install Git:**
   - Run the installer
   - Use **default settings** (recommended)
   - Make sure "Add Git to PATH" is checked ✅

3. **Restart VS Code:**
   - Close VS Code completely
   - Reopen VS Code
   - Open integrated terminal (`Ctrl+``)

4. **Verify Installation:**
   ```bash
   git --version
   ```
   Should show: `git version 2.x.x`

5. **Run Commands:**
   ```bash
   cd "C:\Users\photo\.cursor\projects\BefittingFuneralService"
   git init
   git add .
   git commit -m "first commit: Complete WhatsApp AI system"
   git branch -M main
   git remote add origin https://github.com/optimumimagephotobooth-rgb/Befitting-Funeral-AI-WhatsApp-.git
   git push -u origin main
   ```

---

## ⚡ Faster Option: Use VS Code

**VS Code Source Control is built-in and doesn't require installation!**

See `QUICK_PUSH_VSCODE.md` for VS Code instructions.

---

**Recommendation: Use VS Code Source Control - No installation needed!** 🚀

